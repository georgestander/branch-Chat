services:
  codex:
    build: .
    image: local/codex-cli:latest
    volumes:
      - ~/.codex:/root/.codex          # consider writable so CLI can refresh tokens
      - /Users/georgestander/dev/Connexus:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true

